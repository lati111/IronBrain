<div>
    <div class="pksanc-minimal-block flex flex-col items-center gap-2">
        <div class="flex flex-col items-center gap-0">
            <span class="text-center" title="save file">{{$saveName}}</span>
            <span class="text-center" title="caught game">{{$caughtGame}}</span>
        </div>
        <span class="text-center" title="caught location">{{$caughtLocation}}</span>
        <div class="flex flex-row items-center gap-1">
            <span class="text-center" title="trainer">{{$trainerName}}</span>
            <img src="{{$genderIconPath}}" alt="{{$gender}} gender icon" title="{{$gender}}" class="h-5">
        </div>
    </div>
</div>
